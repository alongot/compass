---
phase: 03-transfer-logic
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/__tests__/useArticulations.test.js
  - src/components/__tests__/transferUtils.test.js
  - src/components/__tests__/TransferView.test.jsx
autonomous: true
requirements:
  - TRANSFER-02
  - TRANSFER-03
must_haves:
  truths:
    - "npm test passes green (all stubs return TODO/pending, none error)"
    - "useArticulations.test.js exercises the hook with a mocked Supabase response"
    - "transferUtils.test.js has failing test stubs for mapCcCoursesToUcsbRequirements"
    - "TransferView.test.jsx has a stub that checks the component renders without crashing"
  artifacts:
    - path: "src/components/__tests__/useArticulations.test.js"
      provides: "Test contract for Supabase-backed articulation hook"
      exports: []
    - path: "src/components/__tests__/transferUtils.test.js"
      provides: "Test contract for pure mapping function"
      exports: []
    - path: "src/components/__tests__/TransferView.test.jsx"
      provides: "Test contract for TransferView component"
      exports: []
  key_links:
    - from: "src/components/__tests__/transferUtils.test.js"
      to: "src/utils/transferUtils.js"
      via: "import"
      pattern: "from.*transferUtils"
    - from: "src/components/__tests__/useArticulations.test.js"
      to: "src/hooks/useArticulations.js"
      via: "import"
      pattern: "from.*useArticulations"
    - from: "src/components/__tests__/TransferView.test.jsx"
      to: "src/components/TransferView.jsx"
      via: "import"
      pattern: "from.*TransferView"
---

<objective>
Create Wave 0 test stub files that define the expected contracts for Phase 3 before any implementation exists.

Purpose: Nyquist rule — every task in subsequent plans must have automated verification. These stubs establish the test files so later plans can run `npm test` and get signal.
Output: Three test files with clearly-commented FAILING stubs (marked `todo`) that will turn green as implementation is completed in Plans 02-04.
</objective>

<execution_context>
@C:/Users/hugot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hugot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-transfer-logic/03-VALIDATION.md

@src/components/__tests__/CourseBrowserView.test.jsx
@src/components/__tests__/EndQuarterModal.test.jsx
@vitest.config.js
@src/test-setup.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useArticulations and transferUtils test stubs</name>
  <files>src/components/__tests__/useArticulations.test.js, src/components/__tests__/transferUtils.test.js</files>
  <behavior>
    - useArticulations.test.js: mock `src/lib/supabase.js` default export, assert that `useInstitutions()` returns mocked institutions array, assert that `useArticulations(id)` returns mocked articulations for that ID — both stubs marked `it.todo` so they are skipped but visible
    - transferUtils.test.js: four `it.todo` stubs for `mapCcCoursesToUcsbRequirements`:
      1. "maps one CC course to a satisfied UCSB requirement"
      2. "returns empty array when no CC courses are checked"
      3. "ignores CC course with no articulation entry"
      4. "does not satisfy requirement when only partial combined articulation is checked"
  </behavior>
  <action>
    Create `src/components/__tests__/useArticulations.test.js`:
    - Import pattern matches existing tests: `import { describe, it, expect, vi } from 'vitest'`
    - Mock supabase: `vi.mock('../../../src/lib/supabase.js', () => ({ supabase: { from: vi.fn() } }))` — adjust relative path to `../../lib/supabase.js` from the __tests__ dir
    - Write two `it.todo` stubs with descriptive names covering TRANSFER-02 contract
    - Add a comment: `// TRANSFER-02: hook reads from Supabase, not a hardcoded local array`

    Create `src/components/__tests__/transferUtils.test.js`:
    - Import: `import { describe, it } from 'vitest'`
    - Write four `it.todo` stubs per the behavior block above
    - Add a comment: `// TRANSFER-03: mapCcCoursesToUcsbRequirements(checkedCcCourses, articulations, majorRequirements) -> string[]`
    - Do NOT import the implementation yet — it does not exist; the import is inside the individual `it` bodies (or deferred to when they are implemented)
  </action>
  <verify>
    <automated>npm test -- src/components/__tests__/useArticulations.test.js src/components/__tests__/transferUtils.test.js</automated>
  </verify>
  <done>Both test files run without error. All cases show as "todo" (skipped), 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create TransferView component test stub</name>
  <files>src/components/__tests__/TransferView.test.jsx</files>
  <action>
    Create `src/components/__tests__/TransferView.test.jsx`:
    - Import pattern: `import { describe, it, expect, vi } from 'vitest'` and `import { render } from '@testing-library/react'`
    - Mock `src/hooks/useArticulations.js` returning empty arrays: `vi.mock('../../hooks/useArticulations.js', () => ({ useInstitutions: () => ({ institutions: [], loading: false }), useArticulations: () => ({ articulations: [], loading: false }) }))`
    - Write two `it.todo` stubs:
      1. "renders CC selector dropdown"
      2. "renders satisfied requirements list when CC courses are checked"
    - Add comment: `// TRANSFER-03: TransferView — CC selector + course checklist + requirements result panel`
    - Do NOT import TransferView yet — it does not exist. Stub bodies reference it but file import is deferred (wrap in a TODO comment or use `it.todo`).

    Verify the pattern by looking at `src/components/__tests__/EndQuarterModal.test.jsx` for the exact mock path style used in this project.
  </action>
  <verify>
    <automated>npm test -- src/components/__tests__/TransferView.test.jsx</automated>
  </verify>
  <done>TransferView.test.jsx runs without error. Both stubs show as "todo" (skipped), 0 failures. npm test overall suite stays green.</done>
</task>

</tasks>

<verification>
Run the full test suite after both tasks: `npm test` — all existing tests pass, three new test files appear with 0 failures (all stubs are `it.todo`).
</verification>

<success_criteria>
- `src/components/__tests__/useArticulations.test.js` exists with 2 todo stubs
- `src/components/__tests__/transferUtils.test.js` exists with 4 todo stubs
- `src/components/__tests__/TransferView.test.jsx` exists with 2 todo stubs
- `npm test` exits 0 — no failures introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-transfer-logic/03-01-SUMMARY.md`
</output>
