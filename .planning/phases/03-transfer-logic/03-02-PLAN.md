---
phase: 03-transfer-logic
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - src/data/articulations/institutions.json
  - src/data/articulations/agreements/sbcc-ucsb.json
  - src/data/articulations/agreements/smc-ucsb.json
  - src/data/articulations/agreements/de-anza-ucsb.json
  - scripts/import-articulations.js
autonomous: true
requirements:
  - TRANSFER-01
  - TRANSFER-02
must_haves:
  truths:
    - "import-articulations.js runs without crashing and logs row counts to stdout"
    - "institutions.json contains all 10 target CCs with correct assist_org_id values"
    - "Three CC agreement files cover enough courses to demo the evaluator (at least 5 articulations each)"
    - "Import script uses upsert so it is re-runnable without duplicate errors"
  artifacts:
    - path: "src/data/articulations/institutions.json"
      provides: "10 CC institution records with assist_org_id"
      contains: "Santa Barbara City College"
    - path: "src/data/articulations/agreements/sbcc-ucsb.json"
      provides: "SBCC articulation rows for target major courses"
      min_lines: 20
    - path: "src/data/articulations/agreements/smc-ucsb.json"
      provides: "SMC articulation rows"
      min_lines: 20
    - path: "src/data/articulations/agreements/de-anza-ucsb.json"
      provides: "De Anza articulation rows"
      min_lines: 20
    - path: "scripts/import-articulations.js"
      provides: "Seeding script for institutions + articulations tables"
      exports: []
  key_links:
    - from: "scripts/import-articulations.js"
      to: "Supabase institutions table"
      via: "supabase.from('institutions').upsert()"
      pattern: "institutions.*upsert"
    - from: "scripts/import-articulations.js"
      to: "Supabase articulations table"
      via: "supabase.from('articulations').upsert()"
      pattern: "articulations.*upsert"
    - from: "src/data/articulations/agreements/*.json"
      to: "scripts/import-articulations.js"
      via: "fs.readFileSync + JSON.parse"
      pattern: "readFileSync.*agreements"
---

<objective>
Author seed data for 10 community colleges and 3 representative CC-to-UCSB articulation agreement files, then write the import script that loads them into Supabase.

Purpose: TRANSFER-01 and TRANSFER-02 — data must be in Supabase (not memory) to survive reload. The import script is a one-time seeding tool that can be re-run safely via upsert.
Output: `institutions.json`, 3 CC agreement JSON files, and `scripts/import-articulations.js`.
</objective>

<execution_context>
@C:/Users/hugot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hugot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-transfer-logic/03-RESEARCH.md
@scripts/import-courses.js
@src/data/migrations/001_initial_schema.sql
</context>

<interfaces>
<!-- Supabase articulations table schema (from 001_initial_schema.sql) -->
```sql
CREATE TABLE institutions (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    name VARCHAR(255) NOT NULL,
    short_name VARCHAR(50),
    institution_type VARCHAR(50),
    assist_org_id VARCHAR(50),
    city VARCHAR(100),
    state VARCHAR(50),
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE articulations (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    source_institution_id UUID NOT NULL REFERENCES institutions(id) ON DELETE CASCADE,
    source_course_code VARCHAR(50) NOT NULL,
    source_course_title VARCHAR(255),
    source_units DECIMAL(4, 2),
    target_course_id UUID NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
    articulation_type VARCHAR(50) DEFAULT 'equivalent'
        CHECK (articulation_type IN ('equivalent', 'partial', 'combined')),
    notes TEXT,
    effective_start_date DATE,
    effective_end_date DATE,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(source_institution_id, source_course_code, target_course_id)
);
```

<!-- courses table lookup key (for resolving target_course_id_clean -> UUID) -->
From courses table: `.eq('course_id_clean', 'MATH 3A')` where course_id_clean is "DEPT NUMBER" format.

<!-- Import script pattern from scripts/import-courses.js -->
```javascript
import { createClient } from '@supabase/supabase-js';
import { readFileSync } from 'fs';
import dotenv from 'dotenv';
dotenv.config();
const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_KEY);
```
Note: existing import scripts use `VITE_SUPABASE_URL` from .env. Add `SUPABASE_SERVICE_KEY` to .env if not present — this bypasses RLS for seeding.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author institutions.json and three CC agreement files</name>
  <files>
    src/data/articulations/institutions.json,
    src/data/articulations/agreements/sbcc-ucsb.json,
    src/data/articulations/agreements/smc-ucsb.json,
    src/data/articulations/agreements/de-anza-ucsb.json
  </files>
  <action>
    Create `src/data/articulations/institutions.json` — array of 10 CC objects:
    ```json
    [
      { "name": "Santa Barbara City College", "short_name": "SBCC", "institution_type": "community_college", "assist_org_id": "92", "city": "Santa Barbara", "state": "CA" },
      { "name": "Santa Monica College", "short_name": "SMC", "institution_type": "community_college", "assist_org_id": "137", "city": "Santa Monica", "state": "CA" },
      { "name": "De Anza College", "short_name": "De Anza", "institution_type": "community_college", "assist_org_id": "113", "city": "Cupertino", "state": "CA" },
      { "name": "Orange Coast College", "short_name": "OCC", "institution_type": "community_college", "assist_org_id": "110", "city": "Costa Mesa", "state": "CA" },
      { "name": "Diablo Valley College", "short_name": "DVC", "institution_type": "community_college", "assist_org_id": "57", "city": "Pleasant Hill", "state": "CA" },
      { "name": "Foothill College", "short_name": "Foothill", "institution_type": "community_college", "assist_org_id": "65", "city": "Los Altos Hills", "state": "CA" },
      { "name": "Pasadena City College", "short_name": "PCC", "institution_type": "community_college", "assist_org_id": "120", "city": "Pasadena", "state": "CA" },
      { "name": "Irvine Valley College", "short_name": "IVC", "institution_type": "community_college", "assist_org_id": "83", "city": "Irvine", "state": "CA" },
      { "name": "College of San Mateo", "short_name": "CSM", "institution_type": "community_college", "assist_org_id": "104", "city": "San Mateo", "state": "CA" },
      { "name": "Laney College", "short_name": "Laney", "institution_type": "community_college", "assist_org_id": "88", "city": "Oakland", "state": "CA" }
    ]
    ```

    Create `src/data/articulations/agreements/sbcc-ucsb.json` — articulation rows for SBCC.
    Each row shape: `{ "source_course_code", "source_course_title", "source_units", "target_course_id_clean", "articulation_type", "notes", "effective_start_date", "effective_end_date" }`
    Include at minimum 8 rows covering courses relevant to the 12 target majors (ECON, CS, BIO, CHEM, MATH, STATS, COMM, SOC, PSYCH, ME, EE, FILM, ENV ST). Focus on gateway courses: MATH 3A/3B, CS 16/24/32, ECON 1/2, BIO 1A, CHEM 1A, PHYS 1.
    Example rows (use actual SBCC equivalents from assist.org website data — these are representative):
    - MATH 150 (Calculus I, 5 units) -> MATH 3A, equivalent
    - MATH 155 (Calculus II, 5 units) -> MATH 3B, equivalent
    - CS 110 (Intro Programming, 4 units) -> CMPSC 16, equivalent
    - ECON 100 (Microeconomics, 3 units) -> ECON 1, equivalent
    - ECON 101 (Macroeconomics, 3 units) -> ECON 2, equivalent
    - BIOL 100 (Cell/Molecular Bio, 4 units) -> MCDB 1A, equivalent
    - CHEM 150 (General Chem I, 5 units) -> CHEM 1A, equivalent
    - PHYS 121 (Physics for Sci/Eng I, 4 units) -> PHYS 1, equivalent
    Set `effective_start_date: "2024-01-01"`, `effective_end_date: null` on all rows.

    Create `src/data/articulations/agreements/smc-ucsb.json` — same structure, SMC equivalents. Include 8+ rows for the same UCSB target courses using SMC course codes (e.g., MATH 7, MATH 8 for Calc I/II; CS 55 for intro programming).

    Create `src/data/articulations/agreements/de-anza-ucsb.json` — same structure, De Anza equivalents. Include 8+ rows (e.g., MATH 1A/1B for Calc I/II; CIS 22A for intro CS).

    Keep `articulation_type` as `"equivalent"` for all rows in this demo (combined articulations are a v2 enhancement per research).
  </action>
  <verify>
    <automated>node -e "const d = JSON.parse(require('fs').readFileSync('src/data/articulations/institutions.json','utf8')); console.assert(d.length === 10, 'need 10 institutions'); const sbcc = JSON.parse(require('fs').readFileSync('src/data/articulations/agreements/sbcc-ucsb.json','utf8')); console.assert(sbcc.length >= 8, 'need >= 8 SBCC rows'); console.log('seed data OK')"</automated>
  </verify>
  <done>institutions.json has 10 records. Each of the 3 CC agreement files has 8+ articulation rows. All rows have required fields: source_course_code, target_course_id_clean, articulation_type.</done>
</task>

<task type="auto">
  <name>Task 2: Write import-articulations.js seeding script</name>
  <files>scripts/import-articulations.js</files>
  <action>
    Create `scripts/import-articulations.js` following the exact pattern of `scripts/import-courses.js`:

    ```javascript
    import { createClient } from '@supabase/supabase-js';
    import { readFileSync, readdirSync } from 'fs';
    import { fileURLToPath } from 'url';
    import { dirname, join } from 'path';
    import dotenv from 'dotenv';
    dotenv.config();

    const supabase = createClient(
      process.env.VITE_SUPABASE_URL,
      process.env.SUPABASE_SERVICE_KEY  // service key bypasses RLS for seeding
    );
    ```

    Script steps (sequential, log each step):
    1. Load `src/data/articulations/institutions.json`, upsert into `institutions` table using `.upsert(rows, { onConflict: 'assist_org_id' })`. Log count inserted.
    2. Re-fetch all institutions to build a `Map<assist_org_id -> UUID>` for FK resolution.
    3. Read all `*.json` files from `src/data/articulations/agreements/` directory. For each file:
       a. Derive the source CC from filename (e.g., `sbcc-ucsb.json` -> look up `assist_org_id` via short_name matching OR embed a `source_short_name` field in the agreement JSON). Use a filename-to-assist_org_id lookup map hardcoded in the script: `{ 'sbcc-ucsb': '92', 'smc-ucsb': '137', 'de-anza-ucsb': '113', ... }`.
       b. Resolve each row's `target_course_id_clean` -> UUID via `.eq('course_id_clean', row.target_course_id_clean)` query on `courses` table. Log any unresolved codes as warnings (do not crash — skip unresolved rows).
       c. Build articulation objects with resolved `source_institution_id` and `target_course_id` UUIDs.
       d. Upsert using `.upsert(rows, { onConflict: 'source_institution_id,source_course_code,target_course_id' })`.
       e. Log: `SBCC: inserted/updated N articulations (M unresolved)`.
    4. Print final summary: total institutions, total articulations, total unresolved.

    Error handling: if `SUPABASE_SERVICE_KEY` is missing from env, print a clear message and exit 1. Each upsert error should log the error message and continue (don't abort the whole run for one CC).

    Use ESM imports (the project uses `"type": "module"` per existing scripts).
  </action>
  <verify>
    <automated>node -e "import('./scripts/import-articulations.js').catch(e => { if(e.message.includes('SUPABASE_SERVICE_KEY')) { console.log('expected env error — script loads OK'); process.exit(0); } throw e; })" 2>&1 | grep -E "OK|error|Error" | head -5</automated>
  </verify>
  <done>scripts/import-articulations.js exists and can be imported without syntax errors. Running it with valid Supabase credentials (SUPABASE_SERVICE_KEY in .env) inserts rows into both tables and logs counts. Upsert is idempotent — running twice does not create duplicates.</done>
</task>

</tasks>

<verification>
Manual smoke test (requires live Supabase credentials):
```
node scripts/import-articulations.js
```
Expected output: "Inserted 10 institutions" then per-CC articulation counts.

Automated seed data validity check:
```
node -e "
  const inst = JSON.parse(require('fs').readFileSync('src/data/articulations/institutions.json','utf8'));
  console.assert(inst.length === 10);
  console.assert(inst.every(i => i.assist_org_id && i.name));
  console.log('OK');
"
```
</verification>

<success_criteria>
- `institutions.json` has 10 CC objects with `assist_org_id`, `name`, `short_name`, `city`, `state`
- Three CC agreement files exist with 8+ articulation rows each
- `scripts/import-articulations.js` is syntactically valid and follows existing script patterns
- Import script uses upsert (idempotent) and logs unresolved course codes as warnings
- npm test stays green (no new test failures)
</success_criteria>

<output>
After completion, create `.planning/phases/03-transfer-logic/03-02-SUMMARY.md`
</output>
