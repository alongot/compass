---
phase: 01-demo-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/components/ProfileWizard.jsx
  - src/components/LoginScreen.jsx
  - src/components/Sidebar.jsx
  - src/components/DashboardView.jsx
  - src/components/RequirementsView.jsx
  - src/components/QuarterPlannerView.jsx
  - src/components/WhatIfView.jsx
  - src/components/RoadmapView.jsx
  - src/components/CourseBrowserView.jsx
  - src/components/LoadingScreen.jsx
autonomous: true
requirements:
  - REFACTOR-01
  - REFACTOR-02

must_haves:
  truths:
    - "Each of the 10 view components is importable from its own file in src/components/"
    - "Every view component imports theme from src/styles/theme.js (not a module-level global)"
    - "Shared components (StatusBadge, DifficultyBadge, ProgressRing, SearchableSelect) are imported from src/components/shared/"
    - "npm run build succeeds after all 10 view files are created"
    - "CompassDemo.jsx is unchanged — views not yet wired up"
  artifacts:
    - path: "src/components/ProfileWizard.jsx"
      provides: "4-step profile creation wizard"
      exports: ["ProfileWizard"]
    - path: "src/components/LoginScreen.jsx"
      provides: "Profile selection screen"
      exports: ["LoginScreen"]
    - path: "src/components/Sidebar.jsx"
      provides: "Navigation sidebar with user menu"
      exports: ["Sidebar"]
    - path: "src/components/DashboardView.jsx"
      provides: "Main dashboard with progress cards and course management"
      exports: ["DashboardView"]
    - path: "src/components/RequirementsView.jsx"
      provides: "Requirements checklist sub-view"
      exports: ["RequirementsView"]
    - path: "src/components/QuarterPlannerView.jsx"
      provides: "Quarter-by-quarter planner sub-view"
      exports: ["QuarterPlannerView"]
    - path: "src/components/WhatIfView.jsx"
      provides: "Major comparison view"
      exports: ["WhatIfView"]
    - path: "src/components/RoadmapView.jsx"
      provides: "Prerequisite map and requirements roadmap"
      exports: ["RoadmapView"]
    - path: "src/components/CourseBrowserView.jsx"
      provides: "Filterable course catalog browser"
      exports: ["CourseBrowserView"]
    - path: "src/components/LoadingScreen.jsx"
      provides: "Loading spinner screen"
      exports: ["LoadingScreen"]
  key_links:
    - from: "src/components/RoadmapView.jsx"
      to: "src/components/RequirementsView.jsx"
      via: "import { RequirementsView }"
      pattern: "import.*RequirementsView"
    - from: "src/components/RoadmapView.jsx"
      to: "src/components/QuarterPlannerView.jsx"
      via: "import { QuarterPlannerView }"
      pattern: "import.*QuarterPlannerView"
    - from: "src/components/CourseBrowserView.jsx"
      to: "src/data/demo/catalogDescriptions.js"
      via: "import { getCatalogDescriptions }"
      pattern: "import.*getCatalogDescriptions"
    - from: "src/components/WhatIfView.jsx"
      to: "src/utils/requirementsUtils.js"
      via: "import { buildUserRequirements }"
      pattern: "import.*buildUserRequirements"
---

<objective>
Extract all 10 view components from CompassDemo.jsx into individual files under src/components/. Each view's imports must be updated to reference the extracted leaf modules, hooks, and shared components created in Plans 01-03.

Purpose: After this plan, every component is independently openable and understandable. Plan 05 will then trim CompassDemo.jsx to a thin composition root.
Output: 10 component files; CompassDemo.jsx still has its original definitions (not yet removed).
</objective>

<execution_context>
@C:/Users/hugot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hugot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-demo-refactor/01-RESEARCH.md
@.planning/phases/01-demo-refactor/01-01-SUMMARY.md
@.planning/phases/01-demo-refactor/01-02-SUMMARY.md
@.planning/phases/01-demo-refactor/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Every import these view components will need — no codebase exploration required -->

Leaf modules (Plan 01):
```js
import { theme } from '../styles/theme.js';
import { extractCourseId, buildKnownCourses, getMajorId } from '../utils/courseUtils.js';
import { buildUserRequirements, buildUserQuarterPlan } from '../utils/requirementsUtils.js';
import { MAJOR_CONFIGS } from '../data/demo/majorConfigs.js';
import { getCatalogDescriptions } from '../data/demo/catalogDescriptions.js';
```

Hooks (Plan 02):
```js
import { useDeptCourses } from '../hooks/useDeptCourses.js';
import { useMajorRequirements } from '../hooks/useMajorRequirements.js';
```

Supabase:
```js
import { supabase } from '../lib/supabase.js';
```

Shared components (Plan 03):
```js
import { StatusBadge } from './shared/StatusBadge.jsx';
import { DifficultyBadge } from './shared/DifficultyBadge.jsx';
import { ProgressRing } from './shared/ProgressRing.jsx';
import { SearchableSelect } from './shared/SearchableSelect.jsx';
```

View component line ranges in CompassDemo.jsx:
- ProfileWizard: lines 1524-2440 (917 lines) — uses theme, MAJOR_CONFIGS, supabase, useDeptCourses, SearchableSelect
- LoginScreen: lines 2444-2553 (110 lines) — uses theme only
- Sidebar: lines 2557-2854 (298 lines) — uses theme only
- DashboardView: lines 2858-3543 (686 lines) — uses theme, StatusBadge, DifficultyBadge, extractCourseId, buildKnownCourses
- RequirementsView: lines 3547-3677 (131 lines) — uses theme, StatusBadge, DifficultyBadge, ProgressRing
- QuarterPlannerView: lines 3681-3834 (154 lines) — uses theme, DifficultyBadge
- WhatIfView: lines 3838-4046 (209 lines) — uses theme, MAJOR_CONFIGS, buildUserRequirements, StatusBadge, ProgressRing
- RoadmapView: lines 4052-4513 (462 lines) — uses theme, RequirementsView, QuarterPlannerView, StatusBadge, DifficultyBadge, ProgressRing
- CourseBrowserView: lines 4514-4807 (294 lines) — uses theme, useDeptCourses, catalogDescriptions (via getCatalogDescriptions), DifficultyBadge, StatusBadge
- LoadingScreen: lines 4811-4834 (24 lines) — uses theme
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract simpler views (LoadingScreen, LoginScreen, Sidebar, RequirementsView, QuarterPlannerView)</name>
  <files>src/components/LoadingScreen.jsx, src/components/LoginScreen.jsx, src/components/Sidebar.jsx, src/components/RequirementsView.jsx, src/components/QuarterPlannerView.jsx</files>
  <action>
Create five component files. Each is a verbatim copy of its line range from CompassDemo.jsx with the appropriate import block added at the top and a named export added. Do NOT remove from CompassDemo.jsx yet.

**Import rules per file:**
- LoadingScreen.jsx (lines 4811-4834): `import React from 'react'; import { theme } from '../styles/theme.js';`
- LoginScreen.jsx (lines 2444-2553): `import React, { useState } from 'react'; import { theme } from '../styles/theme.js';`
- Sidebar.jsx (lines 2557-2854): `import React, { useState, useRef, useEffect } from 'react'; import { theme } from '../styles/theme.js';`
- RequirementsView.jsx (lines 3547-3677): `import React from 'react'; import { theme } from '../styles/theme.js'; import { StatusBadge } from './shared/StatusBadge.jsx'; import { DifficultyBadge } from './shared/DifficultyBadge.jsx'; import { ProgressRing } from './shared/ProgressRing.jsx';`
- QuarterPlannerView.jsx (lines 3681-3834): `import React, { useState } from 'react'; import { theme } from '../styles/theme.js'; import { DifficultyBadge } from './shared/DifficultyBadge.jsx';`

Add `export` keyword before each component's const/function declaration. Verify the line ranges carefully — the component begins at the line shown and ends at the closing `};` or `}` before the next component's comment header.

After creating all five, run the build:
```bash
npm run build
```
  </action>
  <verify>
    <automated>cd /c/Users/hugot/Documents/GitHub/Compass && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Five files exist in src/components/. npm run build is green. Each has a named export matching its component name.</done>
</task>

<task type="auto">
  <name>Task 2: Extract complex views (DashboardView, WhatIfView, RoadmapView, CourseBrowserView, ProfileWizard)</name>
  <files>src/components/DashboardView.jsx, src/components/WhatIfView.jsx, src/components/RoadmapView.jsx, src/components/CourseBrowserView.jsx, src/components/ProfileWizard.jsx</files>
  <action>
Create five more component files. These are larger and have more imports. Same rule: verbatim copy of line range, import block added at top, named export added.

**Import blocks per file:**

DashboardView.jsx (lines 2858-3543):
```jsx
import React, { useState, useRef, useEffect } from 'react';
import { theme } from '../styles/theme.js';
import { StatusBadge } from './shared/StatusBadge.jsx';
import { DifficultyBadge } from './shared/DifficultyBadge.jsx';
import { extractCourseId } from '../utils/courseUtils.js';
```

WhatIfView.jsx (lines 3838-4046):
```jsx
import React, { useState } from 'react';
import { theme } from '../styles/theme.js';
import { MAJOR_CONFIGS } from '../data/demo/majorConfigs.js';
import { buildUserRequirements } from '../utils/requirementsUtils.js';
import { StatusBadge } from './shared/StatusBadge.jsx';
import { ProgressRing } from './shared/ProgressRing.jsx';
```

RoadmapView.jsx (lines 4052-4513):
```jsx
import React, { useState, useEffect, useRef } from 'react';
import { theme } from '../styles/theme.js';
import { RequirementsView } from './RequirementsView.jsx';
import { QuarterPlannerView } from './QuarterPlannerView.jsx';
import { StatusBadge } from './shared/StatusBadge.jsx';
import { DifficultyBadge } from './shared/DifficultyBadge.jsx';
import { ProgressRing } from './shared/ProgressRing.jsx';
```

CourseBrowserView.jsx (lines 4514-4807):
```jsx
import React, { useState, useMemo } from 'react';
import { theme } from '../styles/theme.js';
import { useDeptCourses } from '../hooks/useDeptCourses.js';
import { getCatalogDescriptions } from '../data/demo/catalogDescriptions.js';
import { DifficultyBadge } from './shared/DifficultyBadge.jsx';
import { StatusBadge } from './shared/StatusBadge.jsx';
```

Inside CourseBrowserView, find where `catalogDescriptions` is used (as an object lookup like `catalogDescriptions[courseId]`). Replace all such references with `getCatalogDescriptions()[courseId]` — or assign `const catalogDescriptions = getCatalogDescriptions();` at the top of the component function body to minimize diff.

ProfileWizard.jsx (lines 1524-2440):
```jsx
import React, { useState, useEffect, useRef, useLayoutEffect } from 'react';
import { supabase } from '../lib/supabase.js';
import { theme } from '../styles/theme.js';
import { MAJOR_CONFIGS } from '../data/demo/majorConfigs.js';
import { useDeptCourses } from '../hooks/useDeptCourses.js';
import { SearchableSelect } from './shared/SearchableSelect.jsx';
```

Note: ProfileWizard has `schoolOptions` and `majorOptions` defined at module level just before the component (lines 1518-1523). Include those in the file above the component definition.

After creating all five files, run the build:
```bash
npm run build
```

Fix any "X is not defined" errors — they indicate a missed import. Do not change component logic, only add the import for the missing name.
  </action>
  <verify>
    <automated>cd /c/Users/hugot/Documents/GitHub/Compass && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>All 10 view files exist in src/components/. npm run build is green. Each file is independently openable and contains exactly one named component export.</done>
</task>

</tasks>

<verification>
1. `npm run build` — green
2. `ls src/components/` — shows all 10 view files plus shared/ directory
3. `grep -l "import.*theme" src/components/*.jsx` — every non-shared view imports theme
4. `grep -n "import.*RequirementsView\|import.*QuarterPlannerView" src/components/RoadmapView.jsx` — RoadmapView imports its sub-views from the new files
5. `grep "getCatalogDescriptions" src/components/CourseBrowserView.jsx` — catalog lookup uses the extracted function
6. CompassDemo.jsx line count still at 5086 — not yet modified
</verification>

<success_criteria>
Ten view component files exist under src/components/. npm run build passes. Every component that uses theme imports it from src/styles/theme.js. RoadmapView imports RequirementsView and QuarterPlannerView from their own files. CourseBrowserView uses getCatalogDescriptions() from the data module.
</success_criteria>

<output>
After completion, create `.planning/phases/01-demo-refactor/01-04-SUMMARY.md`
</output>
