---
phase: 04-cc-transfer-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/__tests__/ProfileWizard.test.jsx
  - src/components/__tests__/TransferDashboardView.test.jsx
  - src/components/__tests__/TransferRoadmapView.test.jsx
  - src/components/__tests__/TransferWhatIfView.test.jsx
  - src/components/__tests__/transferUtils.test.js
autonomous: true
requirements:
  - CC-01
  - CC-03
  - CC-04
  - CC-05
  - CC-06

must_haves:
  truths:
    - "npm test passes immediately after Wave 0 (all new tests in todo/pending state, no failures)"
    - "ProfileWizard.test.jsx stubs cover student type choice and transfer user field saving"
    - "TransferDashboardView.test.jsx stubs cover 3 cards rendering for transfer student"
    - "TransferRoadmapView.test.jsx stubs cover IGETC checklist and major req sections"
    - "TransferWhatIfView.test.jsx stubs cover 12 major cards"
    - "transferUtils.test.js has new calculateIgetcProgress test cases (stubbed red)"
  artifacts:
    - path: "src/components/__tests__/ProfileWizard.test.jsx"
      provides: "Test stubs for CC-01"
    - path: "src/components/__tests__/TransferDashboardView.test.jsx"
      provides: "Test stubs for CC-04"
    - path: "src/components/__tests__/TransferRoadmapView.test.jsx"
      provides: "Test stubs for CC-05"
    - path: "src/components/__tests__/TransferWhatIfView.test.jsx"
      provides: "Test stubs for CC-06"
    - path: "src/components/__tests__/transferUtils.test.js"
      provides: "calculateIgetcProgress stubs for CC-03"
  key_links:
    - from: "src/components/__tests__/ProfileWizard.test.jsx"
      to: "src/components/ProfileWizard.jsx"
      via: "import"
      pattern: "import.*ProfileWizard"
    - from: "src/components/__tests__/TransferDashboardView.test.jsx"
      to: "src/components/TransferDashboardView.jsx"
      via: "import (file not yet created — Wave 0 stubs as todo)"
      pattern: "it.todo"
---

<objective>
Create Wave 0 test scaffolds for all Phase 4 requirements so every implementation task has an automated verify command ready.

Purpose: Nyquist compliance — every code-producing task needs an automated test command. Wave 0 stubs create the test files (all tests as it.todo) so that Wave 1/2 tasks can reference them and turn todo stubs green.
Output: 4 new test files + extended transferUtils.test.js, all passing (todo stubs do not fail).
</objective>

<execution_context>
@C:/Users/hugot/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hugot/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cc-transfer-mode/04-CONTEXT.md
@.planning/phases/04-cc-transfer-mode/04-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/hooks/useArticulations.js:
```js
export function useInstitutions() // returns { institutions: [], loading, error }
export function useArticulations(institutionId) // returns { articulations: [], loading, error }; null institutionId → empty array synchronously
```

From src/utils/transferUtils.js:
```js
// existing
export function mapCcCoursesToUcsbRequirements(checkedCcCourses, articulations, majorRequirements)
// to be added in plan 02:
export function calculateIgetcProgress(completedCcCourses, igetcMappings) // returns Set<areaId>
```

From src/components/__tests__/transferUtils.test.js (existing — extend, do not replace):
```js
// Already has tests for mapCcCoursesToUcsbRequirements
// Add calculateIgetcProgress describe block with it.todo stubs
```

Existing test files pattern (use it.todo not it.skip):
```js
import { describe, it, expect } from 'vitest';
describe('FeatureName', () => {
  it.todo('behavior description');
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ProfileWizard and transfer view test stubs</name>
  <files>src/components/__tests__/ProfileWizard.test.jsx, src/components/__tests__/TransferDashboardView.test.jsx, src/components/__tests__/TransferRoadmapView.test.jsx, src/components/__tests__/TransferWhatIfView.test.jsx</files>
  <action>
Create four new test files using it.todo stubs. Do not import the components yet — the components do not exist until later plans. Use describe blocks only with it.todo entries.

ProfileWizard.test.jsx — import ProfileWizard from ../../components/ProfileWizard (already exists); stub:
- it.todo('renders student type choice (UCSB Student / CC Transfer Student) at step 1')
- it.todo('selecting CC Transfer Student and completing wizard saves student_type: "transfer" on user')
- it.todo('transfer path step 2 shows CC dropdown and target major dropdown')
- it.todo('transfer path step 3 shows CC course autocomplete sourced from articulation data')
- it.todo('transfer path calls onComplete with source_institution_id UUID and target_major_id')

TransferDashboardView.test.jsx — import will be from ../../components/TransferDashboardView (does not exist yet); wrap import in a describe that uses it.todo only (no live render calls):
- it.todo('renders three progress cards: unit progress, IGETC progress, major lower-div progress')
- it.todo('unit card shows completed + in-progress units toward 60')
- it.todo('IGETC card shows per-area rows with satisfied/pending status')
- it.todo('major card shows % of target major lower-div requirements satisfied')

TransferRoadmapView.test.jsx:
- it.todo('renders IGETC checklist section with all 10 areas')
- it.todo('renders major lower-division requirements section with satisfied CC course or "Not satisfied" label')
- it.todo('renders unit count summary section toward 60')
- it.todo('shows satisfied areas first within IGETC section')

TransferWhatIfView.test.jsx:
- it.todo('renders all 12 major cards')
- it.todo('highlights current target major with a badge')
- it.todo('sorts majors by % lower-div requirements satisfied descending')
- it.todo('each card shows % satisfied, unit gap, and IGETC delta note')
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose src/components/__tests__/ProfileWizard.test.jsx src/components/__tests__/TransferDashboardView.test.jsx src/components/__tests__/TransferRoadmapView.test.jsx src/components/__tests__/TransferWhatIfView.test.jsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files exist; vitest reports all tests as "todo" (not failing); exit 0</done>
</task>

<task type="auto">
  <name>Task 2: Extend transferUtils.test.js with calculateIgetcProgress stubs</name>
  <files>src/components/__tests__/transferUtils.test.js</files>
  <action>
Read the existing transferUtils.test.js. Append a new describe block at the bottom of the file — do not modify existing tests. Add:

```js
describe('calculateIgetcProgress', () => {
  it.todo('returns empty Set when completedCcCourses is empty');
  it.todo('returns Set with matching area when one course satisfies one area');
  it.todo('returns Set with multiple areas when multiple courses satisfy multiple areas');
  it.todo('ignores courses not in igetcMappings');
  it.todo('is case-sensitive for source_course_code matching');
});
```

Read the file first so the append does not overwrite existing content. Use the Write tool with the full file content (existing content + new describe block).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose src/components/__tests__/transferUtils.test.js 2>&1 | tail -15</automated>
  </verify>
  <done>Existing transferUtils tests still pass; new calculateIgetcProgress describe shows 5 todo stubs; exit 0</done>
</task>

</tasks>

<verification>
Run full test suite to confirm Wave 0 stubs do not break existing tests:

```bash
npm test
```

Expected: all existing tests pass, new stubs appear as "todo".
</verification>

<success_criteria>
- 4 new test files created with it.todo stubs (not it.skip)
- transferUtils.test.js extended with calculateIgetcProgress stubs
- npm test exits 0 — no failures introduced
- Wave 0 verification map in 04-VALIDATION.md is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-cc-transfer-mode/04-01-SUMMARY.md`
</output>
